import { Store } from '@constant/Store.js';

export default class Model {
  constructor() {}
}

// const router = new Router();
// const model = new Model();

// // Page
// notify(RouterEvent.Action, { action: 'move', path: '/' });

// // Router
// subscribe(router, RouterEvent.Action, onReceivedAction);

// router(path, parameter) < -subscribe, notify;
